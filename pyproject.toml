[project]
name = "django-crm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django<=6.0",
]

[dependency-groups]
dev = [
    "pip>=26.0.1",
]
